groups:
  - name: node-alerts
    rules:
      - alert: NodeDown
        expr: up{job="node"} == 0
        for: 2m
        labels:
          severity: critical
        annotations:
          summary: "Node Exporter Down"
          description: "{{ $labels.server }} ({{ $labels.instance }}) is down."